<h1>Name: <%= @merchant.name %></h1>

<div id="merchant-items">
  <h2>Items:</h2>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to item.name, item_path(item.id), method: :get %></li>
    <% end %>
  </ul>
</div>